# Act configuration file for local GitHub Actions testing
# See: https://nektosact.com/usage/index.html

# Use medium-sized runner image (good balance of size vs compatibility)
--platform ubuntu-latest=catthehacker/ubuntu:act-latest

# Set container architecture for Apple M-series chips and cross-platform compatibility
--container-architecture linux/amd64

# Use local environment file if it exists
--env-file .env.local

# Use local secrets file if it exists
--secret-file .secrets.local

# Reuse containers to speed up subsequent runs
--reuse

# Set default event (can be overridden on command line)
--eventpath .github/events/push.json

# Network settings for services (like PostgreSQL in integration tests)
--network host

# Useful environment variables for local testing
--env CI=true
--env GITHUB_ACTIONS=true
--env RUNNER_OS=Linux
--env RUNNER_ARCH=X64
